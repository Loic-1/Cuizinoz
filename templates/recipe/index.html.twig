{% extends 'base.html.twig' %} {# {% block stylesheets %}

<link rel="stylesheet" href="{{ asset('css/listRecipe.css') }}" />

{% endblock %} #}

{% block javascripts %}

  {{ parent() }}
  <script src="{{ asset('js/script.js') }}"></script>

{% endblock %}

{% block title %}Nos recettes | Cuizinoz{% endblock %}

{% block body %}
  
<div class="wrapper">
  <h1>Nos recettes</h1>
    
  {% include "components/_recipeContainer.html.twig" with { recipes: recipes, filterForm: filterForm } only %}  

  {% if app.user %}
    <a href="{{ path('create_recipe') }}" class="addRecipe_btn">
      <i class="fa-regular fa-plus"></i> <span>Créer une recette</span>
    </a>
  {% endif %}

</div>
{% endblock %}
