{% extends 'base.html.twig' %}

{# {% block stylesheets %}

  <link rel="stylesheet" href="{{ asset('css/listRecipe.css') }}" />

{% endblock stylesheets %} #}

{% block javascripts %}

  <script src="{{ asset('js/script.js') }}"></script>

{% endblock javascripts %}

{% block meta_description %}

  <meta name="description" content="{{ metaDescription }}">

{% endblock meta_description %}

{% block title %}Recettes {{ user.pseudo }}{% endblock title %}

{% block body %}

<div class="wrapper">
  <nav aria-label="Breadcrumb" class="breadcrumb">
    <ol>
      <li><a href="{{ path('app_home') }}">Accueil</a></li>
      <li><a href="{{ path('detail_user', { user: user.id }) }}">{{ user.pseudo }}</a></li>
      <li><span aria-current="page"></span>Liste recettes</li>
    </ol>
  </nav>

  <h1>Recettes {{ user.pseudo }}</h1>

  {% include "components/_recipeContainer.html.twig" with { recipes: recipes, filterForm: filterForm } only %}
  
</div>

{% endblock body %}
